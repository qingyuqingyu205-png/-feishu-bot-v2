[project]
name = "feishu-bot"
version = "0.1.0"
description = "飞书机器人"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv>=1.2.2",
    "lark-oapi>=1.5.4",
]

[tool.uv]
[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
